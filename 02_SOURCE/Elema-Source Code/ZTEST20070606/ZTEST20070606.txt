REPORT ZTEST20070606 .

TABLES TCURC.
SELECT-OPTIONS S_WAERS FOR TCURC-WAERS NO INTERVALS.

AT SELECTION-SCREEN.

LOOP AT S_WAERS.

SELECT SINGLE *
FROM TCURC
WHERE WAERS = S_WAERS-LOW.

IF SY-SUBRC <> 0.
MESSAGE E614(Z1) WITH '’Ê‰Ý'.
ENDIF.
ENDLOOP.
