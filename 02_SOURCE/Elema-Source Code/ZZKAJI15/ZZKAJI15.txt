REPORT ZZKAJI15 .

data:T_ZDUMMY TYPE STANDARD TABLE OF ZDUMMY ,"支払予定（仕入）
W_ZDUMMY TYPE ZDUMMY.                   "支払予定（仕入）

data:T_ZN002  TYPE STANDARD TABLE OF ZN002  ,"支払予定（仕入）
W_ZN002  TYPE ZN002.                   "支払予定（仕入）

data:T_ZDUMMY2 TYPE STANDARD TABLE OF ZDUMMY2 ,
W_ZDUMMY2 TYPE ZDUMMY2.

data:T_ZN005  TYPE STANDARD TABLE OF ZN005  ,
W_ZN005  TYPE ZN005.

TABLES: LFA1,KNA1.
PARAMETERS:P_BUKRS TYPE BUKRS.

SELECT-OPTIONS S_LIFNR FOR LFA1-LIFNR OBLIGATORY.
SELECT-OPTIONS S_KUNNR FOR KNA1-KUNNR OBLIGATORY.

START-OF-SELECTION.

REFRESH:T_ZDUMMY,T_ZDUMMY2,
T_ZN002,T_ZN005.

*テーブル読み込み
SELECT * INTO TABLE T_ZDUMMY
FROM ZDUMMY
WHERE BUKRS = P_BUKRS
AND LIFNR IN S_LIFNR.

SELECT * INTO TABLE T_ZN002
FROM ZN002
WHERE BUKRS = P_BUKRS
AND LIFNR IN S_LIFNR.

SELECT * INTO TABLE T_ZDUMMY2
FROM ZDUMMY2
WHERE BUKRS = P_BUKRS
AND KUNNR IN S_KUNNR.

SELECT * INTO TABLE T_ZN005
FROM ZN005
WHERE BUKRS = P_BUKRS
AND KUNNR IN S_KUNNR.

*テーブルからデータ削除
DELETE FROM ZN002 WHERE BUKRS = P_BUKRS
AND LIFNR IN S_LIFNR.

DELETE FROM ZDUMMY WHERE BUKRS = P_BUKRS
AND LIFNR IN S_LIFNR.

DELETE FROM ZN005 WHERE BUKRS = P_BUKRS
AND KUNNR IN S_KUNNR.

DELETE FROM ZDUMMY2 WHERE BUKRS = P_BUKRS
AND KUNNR IN S_KUNNR.

*テーブルデータ挿入
INSERT ZN002 FROM TABLE T_ZDUMMY
ACCEPTING DUPLICATE KEYS.

INSERT ZDUMMY FROM TABLE T_ZN002
ACCEPTING DUPLICATE KEYS.

INSERT ZN005 FROM TABLE T_ZDUMMY2
ACCEPTING DUPLICATE KEYS.

INSERT ZDUMMY2 FROM TABLE T_ZN005
ACCEPTING DUPLICATE KEYS.

* 結果
WRITE:/1 '完了'.
